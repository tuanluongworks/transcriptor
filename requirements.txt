# Core dependencies
numpy>=1.24.0
scipy>=1.10.0

# Audio capture (Windows WASAPI)
pyaudiowpatch>=0.2.12.5

# GPU-accelerated Whisper
# IMPORTANT: CTranslate2 4.5+ requires CUDA 12.x
# Install PyTorch separately with CUDA 12.4:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# Then install other requirements:
#   pip install -r requirements.txt
ctranslate2>=4.5.0  # Requires CUDA 12.x and cuDNN 9.x
faster-whisper>=1.2.0  # Compatible with CTranslate2 4.5+

# Voice Activity Detection
webrtcvad>=2.0.10

# Optional: Ollama integration
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
colorama>=0.4.6
